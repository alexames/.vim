# Linux

```
cd ~
echo "so ~/.vim/vimrc" > ~/.vimrc
sudo apt install git vim cmake curl build-essential vim-nox python3-dev mono-complete golang nodejs default-jdk npm
git clone --recursive https://github.com/alexames/.vim.git
vim
```
